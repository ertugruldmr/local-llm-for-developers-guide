# Shared deps for every package. Python 3.11.
# Per-package extras (chromadb for 03-rag-feedback, transformers+torch for the
# DistilBERT comparison in 04-fullstack-analyzer) live in that package's own
# requirements, not here.

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
openai>=1.40,<2.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0

# test / dev
pytest>=8.0,<9.0
httpx>=0.27,<1.0          # FastAPI TestClient transport
